package com.rudra;

public class Main {
    public static void main(String[] args) {
        // Syntax
        // datatype[] variable_name = new datatype[size];
//  the datatype in the syntax represent the datatype of the data stored inside the array

        int[] ros; // declaration of array. ros is getting defined in the stack.
        ros = new int[1]; // actually here is object is being created in the memory (heap).
    }
}